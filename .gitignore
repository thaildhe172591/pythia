# Credentials & local config — never commit
connections.json
*.local.json
.env
.pythia/

# Internal planning material — briefs, specs and plans. These name deployments
# and hosts that must never reach a public repo, and none of them is part of the
# shipped product, so the whole tree stays out.
docs/
tool-oracle-v1/
NOTES-research.md

# Python
__pycache__/
*.pyc
