# Credentials & local config — never commit
connections.json
*.local.json
.env
.pythia/

# Internal planning material — briefs, specs and plans. These name deployments
# and hosts that must never reach a public repo, and none of them is part of the
# shipped product, so the whole tree stays out.
docs/
tool-oracle-v1/
NOTES-research.md

# Local dogfood project material (developed WITH pythia, not part of it).
# The conventions document is measured on a live company schema and names
# its host, schemas and tenants — it is the single most leak-prone file
# anyone will ever put in this working copy.
sql/
*_ORACLE_CONVENTIONS.md

# Agent-installed skill copies (the canonical pack lives in skills/)
.claude/
.agents/
skills-lock.json

# Python
__pycache__/
*.pyc

# Packaging build artifacts
build/
dist/
*.egg-info/
